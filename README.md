# TelePrompt One Pro

Android-приложение телесуфлёра с overlay-поддержкой для отображения прокручивающегося текста поверх камеры.

## ✨ Основные возможности

- **Overlay-суфлёр** - отображение текста поверх любых приложений, включая камеру
- **Автопрокрутка** - плавная автоматическая прокрутка текста с регулируемой скоростью
- **Адаптивные layouts** - автоматическое переключение между портретным и ландшафтным режимами
- **Drag & Drop** - перетаскивание окна overlay в любое место экрана
- **Редактор сценариев** - создание, редактирование и сохранение текстов
- **Foreground Service** - постоянная работа overlay без выгрузки системой

## 🏗️ Архитектура

Проект следует принципам Clean Architecture:

```
app/
├── ui/              # UI слой (Activities, Compose, Service)
├── core/            # Бизнес-логика (Controllers)
├── data/            # Слой данных (Room, Models)
└── utils/           # Утилиты и константы
```

## 🛠️ Технологии

- **Kotlin** - основной язык
- **Jetpack Compose** - UI для активностей
- **Room** - локальное хранилище сценариев
- **DataStore** - сохранение настроек
- **WindowManager** - overlay поверх других окон
- **Foreground Service** - постоянная работа overlay

## 🚀 Сборка проекта

1. Откройте проект в Android Studio
2. Синхронизируйте Gradle: `File -> Sync Project with Gradle Files`
3. Запустите приложение: `Run -> Run 'app'`

## 📱 Требования

- **Min SDK**: 26 (Android 8.0)
- **Target SDK**: 34 (Android 14)
- **Разрешения**:
  - `SYSTEM_ALERT_WINDOW` - для overlay
  - `FOREGROUND_SERVICE` - для постоянной работы
  - `POST_NOTIFICATIONS` - уведомления (Android 13+)

## 🎯 Использование

### 1. Предоставление разрешений

При первом запуске приложение запросит разрешение на отображение поверх других окон.

### 2. Создание сценария

1. Нажмите кнопку `+` на главном экране
2. Введите название и текст сценария
3. Нажмите `Save`

### 3. Запуск overlay

1. Выберите сценарий из списка
2. Нажмите `Show Overlay`
3. Откройте камеру - текст будет виден поверх

### 4. Управление overlay

- **▶️ Play/Pause** - запуск/остановка автопрокрутки
- **− / +** - уменьшение/увеличение скорости прокрутки
- **⤢ Drag handle** - перетаскивание окна
- **❌ Close** - закрытие overlay

## 🔄 Смена ориентации

Overlay автоматически адаптируется при повороте экрана:
- **Portrait**: текст по центру, кнопки снизу
- **Landscape**: текст слева (70%), кнопки справа вертикально

## 📝 Структура файлов

### Ключевые компоненты:

- `TeleprompterOverlayService.kt` - сердце overlay-функциональности
- `ScrollController.kt` - управление автопрокруткой
- `OverlayController.kt` - управление WindowManager
- `MainActivity.kt` - главный экран со списком сценариев
- `ScriptEditorActivity.kt` - редактор текстов

### XML Layouts:

- `overlay_portrait.xml` - вертикальный layout
- `overlay_landscape.xml` - горизонтальный layout

## 🎨 Customization

### Изменение скорости прокрутки

В `Constants.kt`:
```kotlin
const val DEFAULT_SCROLL_SPEED = 50 // пиксели/секунду
const val MIN_SCROLL_SPEED = 10
const val MAX_SCROLL_SPEED = 200
```

### Изменение размера шрифта

В `Constants.kt`:
```kotlin
const val DEFAULT_FONT_SIZE = 28 // sp
```

### Изменение прозрачности фона

В `overlay_portrait.xml` и `overlay_landscape.xml`:
```xml
android:background="#80000000"
```
(80 = 50% прозрачность)

## 🐛 Troubleshooting

### Overlay не отображается

1. Проверьте разрешение `SYSTEM_ALERT_WINDOW` в настройках Android
2. Убедитесь, что Service запущен (должно быть уведомление)

### Прокрутка не работает

1. Нажмите кнопку Play ▶️
2. Проверьте, что скорость > 0

### Текст не обновляется при смене ориентации

Service автоматически пересоздаёт layout - это нормальное поведение.

## 📄 Лицензия

MVP Project - TelePrompt One Pro

## 👨‍💻 Разработка

Проект создан как MVP для демонстрации overlay-функциональности Android.

**Следующие шаги для улучшения:**
- Добавить настройки шрифта и цветов
- Реализовать импорт текстов из файлов
- Добавить голосовое управление
- Реализовать зеркальное отображение текста
- Добавить таймер и маркеры в тексте
